---
tags: [plan, import, implementation]
created: 2026-04-21
status: active
related: [[Импорт — проблемы и решения]], [[Идеи для импорта]]
---

# План реализации И-08 (модератор импорта)

Пошаговая декомпозиция И-08 в инженерные задачи. Все дизайн-вопросы (Q1–Q7) закрыты — см. [[Идеи для импорта#И-08]]. Этот план — про **как делать**, не про **что делать**.

## Принципы исполнения

- **Итеративность.** Каждая фаза даёт работающий, развёрнутый инкремент. Не держать ветку месяцами — мерджить по фазам.
- **Обратная совместимость в пределах фазы.** Старые импорты не ломаются, пока не переехали на новую логику.
- **Retro-инвариант** (из [[../../../memory/project_import_moderator|project memory]]): прошлые транзакции не переклассифицируются никогда. Любая новая логика — только на новые импорты.
- **Без LLM должно работать.** Детерминированный слой (Phases 1–3) — самостоятельная ценность. Phase 4 (LLM) — усиление, не обязательная зависимость.
- **Покрытие тестами на реальных данных.** У пользователя есть живые выписки (Яндекс Сплит, Т-Банк с договорами, кредитки) — они и есть golden dataset.

## Общая последовательность

```
Phase 0 (чистка)
   ↓
Phase 1 (нормализация)   Phase 2 (модель правил)    ← можно параллельно
   ↓                            ↓
   └────────────┬───────────────┘
                ↓
        Phase 3 (кластеры + parked)
                ↓
        Phase 4 (LLM-модератор)
                ↓
        Phase 5 (UI wizard)
                ↓
        Phase 6 (калибровка, метрики)
                ↓
        Phase 7 (уборка legacy)
```

---

## Phase 0. Подготовка

**Цель:** чистый старт без смешивания с предыдущей работой.

### Задачи

- [x] **0.1** Разобрать текущие uncommitted изменения (11 файлов из `git status`). Что из этого — отдельная завершённая работа, которую надо закоммитить перед стартом? Что — эксперименты, которые надо откатить? Что — уже частично под И-08 и должно продолжиться в рамках фазы?
- [x] **0.2** Создать ветку `feature/import-moderator`. Все следующие фазы — в ней (или серия sub-веток под фазы).
- [x] **0.3** Сохранить образцы реальных выписок в `tests/fixtures/statements/` (по одному файлу на каждый сценарий: Яндекс Банк с неявным направлением, Яндекс Сплит с многострочным переводом, Т-Банк с номерами договоров, кредитная карта с погашениями, простой CSV с +/-). Этот fixture-набор становится golden dataset для Phases 1–6.
  - **Структура создана.** `tests/fixtures/statements/{raw,expected}/` + README. `raw/` в `.gitignore` (PII — реальные выписки не коммитятся). `expected/*.expected.json` — скелеты со `status: "pending_raw_fixture"`, идентификаторы в формате `sha256(value)[:16]`, ФИО как `"PRESENT"`. Наполнение raw + expected — итеративно по ходу Phase 1.4, не блокирует Phase 1.1/1.2 (код нормализатора + схема пишутся/тестируются на синтетических входах).

**Acceptance:** ветка создана, образцы в репо, `git status` чист перед началом Phase 1.

---

## Phase 1. Инфраструктура нормализации (Q1)

**Цель:** каждая строка импорта после нормализации имеет структурированные идентификаторы и текстовый `skeleton` для fingerprint-группировки.

### Задачи

- [x] **1.1** Модуль `app/services/import_normalizer_v2.py` (или рефактор существующего). Функции:
  - `extract_tokens(description) -> ExtractedTokens` — regex вырывает phone, contract, iban, card, person_name, amounts, dates.
  - `normalize_skeleton(description, extracted) -> str` — lowercase, удаление пунктуации/стоп-слов, схлопывание пробелов, замена вытащенных токенов плейсхолдерами в тексте.
  - `fingerprint(bank, account_id, direction, skeleton, contract) -> str` — стабильный хеш.
- [x] **1.2** Расширить JSON-структуру `ImportRow.normalized_data_json` новыми полями: `skeleton`, `tokens.{phone, contract, iban, card, person_name, counterparty_org}`, `fingerprint`. Схема — в [[../../14-Specifications/Import-Normalized-Data-Schema|отдельная спека]] (создать при необходимости).
- [x] **1.3** Интеграция в `ImportService`: после существующего normalizer вызывается новый `normalizer_v2`, результат пишется в `normalized_data_json`.
- [~] **1.4** Тесты на golden dataset: каждый fixture-файл → ожидаемые extracted tokens + skeleton. Точные expected-значения пишем руками на 10–20 строках каждого банка. **Infra-only, raw pending** (2026-04-22): инфраструктура (генератор + параметризованный тест со скипом + unit-тест скрипта) готова, реальные raw-выписки ещё не положены в `tests/fixtures/statements/raw/` — все golden-параметры скипаются. Наполнение идёт итеративно по мере появления выписок.

**Acceptance:** прогон нормализатора на всех fixture'ах выдаёт корректные tokens и устойчивый skeleton. Две строки с одним и тем же номером договора получают одинаковый fingerprint (если остальные параметры совпадают), с разными — разный.

**Риски:** регулярки для ФИО — особенно хрупкие (русский язык, сокращения, инициалы). Стартовать с широкой регулярки, потом сужать по ошибкам на golden dataset.

---

## Phase 2. Модель правил со strength (Q3, Q4)

**Цель:** `TransactionCategoryRule` умеет confirms/rejections/scope/is_active; миграция существующих данных прошла с порогом `hit_count ≥ 5`.

### Задачи

- [x] **2.1** Alembic-миграция `XXXX_rule_strength`:
  - Добавить поля: `rejections: int DEFAULT 0 NOT NULL`, `scope: enum('exact','bank','global','legacy_pattern') DEFAULT 'legacy_pattern'`, `is_active: bool DEFAULT false`, `bank_code: varchar NULL`, `account_id_scope: int NULL (FK accounts)`, `identifier_key: varchar NULL` (для exact scope — это phone/contract/…), `identifier_value: varchar NULL`.
  - Data migration: для всех существующих записей — `scope='legacy_pattern'`, `is_active = (hit_count >= 5)`, остальные поля null.
  - Переименовать `hit_count` в `confirms` (keep old as computed alias, если в коде много ссылок — решить при написании).
- [x] **2.2** Обновить `TransactionCategoryRule` модель, `TransactionCategoryRuleRepository`.
- [x] **2.3** Новый сервис `rule_strength_service.py`:
  - `on_confirmed(rule_id)` — `confirms++`, при пересечении порогов активирует/обобщает scope.
  - `on_rejected(rule_id)` — `rejections++`, при пересечении порогов/error-ratio деактивирует.
  - Пороги вынесены в `settings` (`RULE_ACTIVATE_CONFIRMS=2`, `RULE_GENERALIZE_CONFIRMS=3`, `RULE_DEACTIVATE_REJECTIONS=3`, `RULE_ERROR_RATIO_CAP=0.3`).
- [x] **2.4** Интеграция точек учёта confirm/reject:
  - Коммит импорта без правки → confirm для применённого правила.
  - Правка категории/типа/счёта в UI перед коммитом → reject.
  - Правка закоммиченной транзакции в окне ≤7 дней → reject.
- [x] **2.5** Endpoint `GET /rules` с фильтрами по scope/is_active. Frontend страница «Мои правила» — отдельная задача в Phase 5, пока только API.

**Acceptance:** миграция применяется без потери данных; 48% правил активны; unit-тесты на переходы между состояниями strength проходят; учёт confirm/reject работает на E2E-сценарии «импорт → коммит → правка».

**Риски:** переименование `hit_count` → `confirms` может задеть много мест. Альтернатива — оставить `hit_count` как legacy-поле, синхронизировать с `confirms` в одну сторону.

---

## Phase 3. Кластеризация + parked-контейнер

**Цель:** импорт-сессия группирует строки в кластеры по fingerprint; неуверенные строки можно отложить в `parked`.

### Задачи

- [x] **3.1** Модель/схема кластера. Решение: отдельная таблица `import_clusters` ИЛИ виртуальная группировка в runtime (по fingerprint). Начать с runtime-группировки — не создавать лишнюю таблицу, пока не поняли нужна ли персистентность. Если Phase 4 (Celery-task с per-cluster status) потребует — тогда делаем таблицу.
- [x] **3.2** В `ImportService` после Phase 1-нормализации — функция `build_clusters(session) -> list[Cluster]`: группирует `ImportRow` по fingerprint, возвращает агрегаты (count, total amount, example rows, candidate rules).
- [x] **3.3** Применение правил к кластеру: проверить, есть ли в `TransactionCategoryRule` активные правила с совпадающим `identifier_value` (для exact scope) или `fingerprint`-подобным паттерном (для bank/global). Если да — предложенная категория/тип + confidence.
- [x] **3.4** Расчёт cluster confidence: максимум из source-признаков (применённое правило, детерминированный rule-hit по ключевым словам, совпадение пары возвратов, совпадение transfer-кандидата).
- [x] **3.5** Новый статус `ImportRow.status = 'parked'`. Отдельная миграция добавляет enum-значение (если столбец — enum) или просто строка допустима (если строка).
- [x] **3.6** Endpoints: `POST /imports/rows/{id}/park`, `POST /imports/rows/{id}/unpark`, `GET /parked-queue` (глобальный список parked-строк across sessions).
- [x] **3.7** Фильтрация `parked` во всех агрегациях: `metrics_service.py`, `financial_health_service.py`, любые income/expense aggregations в repository. Acceptance-тест: parked-строка не попадает ни в Поток, ни в FI-score, ни в DTI, ни в Health.
- [x] **3.8** Refund matcher: `app/services/refund_matcher_service.py` — парный матчинг одинаковой суммы с противоположными направлениями в окне ±14 дней, плавающая confidence (0.95 / 0.8 / 0.6). Бустеры: совпадающий counterparty_org, ключевые слова, совпадение `source_reference`.

**Acceptance:** импорт fixture'ов Яндекс Сплит (многострочный перевод) и Т-Банк (разные договора) даёт корректное число кластеров. Возврат на ту же сумму через 5 дней обнаруживается как пара. Parked-строки не влияют ни на одну аналитическую метрику.

**Риски:** многострочный перевод из Яндекс Сплит (17569 + 431 = 18000) — это отдельный матчер, не помещается в стандартную схему fingerprint. Либо доработка transfer_matcher для суммарных пар, либо отдельный `split_transfer_matcher`. В первой итерации — оставить нерешённым, записать в Phase 6 как расширение.

---

## Phase 4. LLM-модератор (Q5, Q6, Q7)

**Цель:** неуверенные кластеры (confidence < 0.7) обогащаются гипотезой LLM; работает асинхронно через Celery с per-cluster гранулярностью.

### Задачи

- [x] **4.1** Рефакторинг `app/services/llm_service.py` в структуру `app/services/llm/`:
  - `llm/base.py` — ABC `LLMProvider` с методом `generate_structured(system, user, schema, *, max_tokens, cache_key) -> BaseModel | None`.
  - `llm/anthropic_provider.py` — текущая логика за вычетом бизнес-уровня.
  - `llm/registry.py` — `get_provider()` по `settings.LLM_PROVIDER`.
  - Наверху остаётся `llm_service.py` как domain-layer, про Anthropic не знает.
- [x] **4.2** Новый сервис `import_moderator_service.py` с методом `moderate_cluster(cluster) -> ClusterHypothesis`. Строит промпт:
  - System: роль + формат вывода + правила confidence.
  - User: анонимизированные примеры строк (идентификаторы заменены плейсхолдерами), контекст счёта, список категорий пользователя, релевантные активные правила.
  - Output schema (Pydantic): `operation_type`, `direction`, `predicted_category_id`, `confidence`, `reasoning`, `follow_up_question` (если LLM сам неуверен — сформулирует вопрос пользователю).
- [x] **4.3** Per-cluster хранение гипотезы: поле `moderation_status` + `llm_hypothesis` в runtime-кластере (Phase 3) или в `ImportRow.normalized_data_json` (один на первую строку кластера). Решение — при реализации.
- [x] **4.4** Celery-task `moderate_import_session(session_id)`:
  - Проходит по кластерам с `confidence < 0.7`.
  - Для каждого вызывает `moderator.moderate_cluster(...)` и пишет результат.
  - Статус сессии: `moderation_status` = `pending` → `running` → `ready` / `failed`.
- [x] **4.5** Endpoint `GET /imports/{id}/moderation-status`: `{status, progress, clusters: [{cluster_id, status, hypothesis?}]}`. Per-cluster гранулярность — уже заложена (из решения Q6).
- [x] **4.6** Fallback-режим: LLM недоступен / сбой / `is_enabled=False` → кластеры получают `moderation_status='skipped'`, wizard всё равно открывается, гипотез нет — ручной разбор.
- [x] **4.7** Анонимизация в промпте: использовать `skeleton` из Phase 1 + явные плейсхолдеры `<PHONE>`, `<CONTRACT>`, `<IBAN>`, `<CARD>`, `<PERSON_NAME>`. Юнит-тест: в payload, улетающем в LLM, нет сырых идентификаторов.
- [x] **4.8** Prompt caching: `cache_control: ephemeral` на system-промпте + списке категорий (стабильная часть между сессиями пользователя).

**Acceptance:** Celery-task на реальной сессии с 20 кластерами завершается за < 30 секунд; гипотезы сохраняются per-cluster; при выключении LLM (`LLM_CLASSIFICATION_ENABLED=false`) импорт работает, кластеры без гипотез; в payload'е нет идентификаторов (проверяется тестом).

**Риски:** стоимость на пользователя. Надо в Phase 6 замерить: средний расход токенов за сессию. Заложить бюджетный gauge в метриках.

---

## Phase 5. UI модератора в import-wizard

**Цель:** пользователь видит кластеры с гипотезами, подтверждает/отклоняет одним кликом, может отложить в parked.

### Задачи

- [x] **5.1** Новая стадия `review` в [frontend/components/import/import-wizard.tsx](../../../frontend/components/import/import-wizard.tsx) между `preview` и `commit`.
- [x] **5.2** Экран ожидания: polling `/moderation-status` каждые 2с, прогресс-бар. Переход на review при `status=ready` или `failed`.
- [x] **5.3** Карточка кластера:
  - Header: `<count> похожих строк, <total amount> RUB, <bank>/<account>`.
  - 3 примера строк (+ «показать все N»).
  - Гипотеза LLM (если есть): предложенная категория / operation_type / direction + reasoning.
  - Кнопки: `Подтвердить` / `Изменить` (меняет категорию/тип/счёт, потом Подтвердить) / `Отложить` (parked) / `Пропустить в коммит как есть`.
- [x] **5.4** UI правил со strength: на карточке подтверждённого кластера — мелким текстом «будет создано правило для банка <X>».
- [x] **5.5** Страница «Мои правила» (новая): список активных/неактивных/legacy-правил, фильтры по scope, кнопки «Включить/Выключить/Удалить».
- [x] **5.6** Страница «Недоразобранное» (parked queue): глобальный список across sessions, с кнопкой «Разобрать сейчас» → возвращает строку в wizard-режим одиночной обработки.
- [x] **5.7** Счётчик parked на главной/в сайдбаре.

**Acceptance:** пройти import fixture'а Т-Банк end-to-end: загрузка → ожидание → review с кластерами и гипотезами → подтвердить все → commit → транзакции в базе, правила созданы с `confirms≥1`, scope правильный.

**Риски:** поиск/редактирование правил — может раздуться в отдельную крупную задачу. Минимальный скоуп на Phase 5: просмотр + включение/выключение + удаление. Редактирование сложных полей — отдельная фаза.

---

## Phase 6. Калибровка, метрики, тесты

**Цель:** понять, насколько точно система работает; подкрутить пороги.

### Задачи

- [x] **6.1** Метрики модерации (в `metrics_service` или новый `moderation_metrics`):
  - Доля кластеров с confidence ≥0.9 (молча).
  - Доля с 0.7–0.9 (одним кликом).
  - Доля с <0.7 (вопрос LLM).
  - Средняя confidence после LLM-обработки.
  - Средняя токен-стоимость сессии.
  - Счётчик parked-строк.
- [x] **6.2** Прогон всех fixture'ов через новую pipeline. Ручная сверка ожиданий. **(Сделано на синтетических rows; golden raw ещё в pending-state из 1.4 — полный прогон на реальных выписках — по мере их появления. Синтетический E2E покрывает весь flow normalizer→cluster→moderator.)**
- [~] **6.3** Калибровка порогов confidence-gate (0.7 / 0.9) и strength (5 / 3) на реальных данных. Зафиксировать в settings. **(Отложена — пороги оставлены на стартовых значениях; калибровка требует реальных сессий модератора. Пороги выведены в `settings.RULE_*` и в `CONFIDENCE_*` константы — править без миграции.)**
- [~] **6.4** Многострочный перевод (Яндекс Сплит): расширение transfer_matcher для сумм-пар. **Отложено в будущую фазу (И-09)** — механизм подбора сумм-пар не проектировался, синтетический flow пока не покрывает, риск ложных срабатываний высокий. Зафиксировано как отдельная задача.
- [x] **6.5** Honest-warning проверка: имитируем ситуацию «система в серой зоне не уверена» — убеждаемся, что UI показывает явный warning, не тихий автоответ.

**Acceptance:** метрики подключены; на golden dataset показатели «правильности» — 100% по проверенным кластерам (или явный warning там, где неуверенность).

---

## Phase 7. Уборка legacy

**Цель:** удалить параллельную старую логику автоприменения правил, оставить только новую модель.

### Задачи

- [ ] **7.1** Удалить старые ветки кода в `transaction_enrichment_service.py`, которые применяли правила без strength-модели.
- [ ] **7.2** Завершить переименование `hit_count` → `confirms` если этого ещё не сделали в Phase 2.
- [ ] **7.3** Все `legacy_pattern`-правила, не активированные/не использованные за 180 дней после релиза → отдельной миграцией пометить `is_active=False` (если ещё не) или удалить (согласовать с пользователем перед этим шагом).
- [ ] **7.4** Обновить CLAUDE.md: новая архитектура импорта, правила со strength, модератор.

**Acceptance:** нет параллельных кодовых путей; `git grep hit_count` возвращает только тесты/миграции.

---

## Открытые технические вопросы реализации

_(всплывут по ходу, добавлять сюда)_

- Схема хранения кластера (таблица vs runtime) — решаем в Phase 3.
- Многострочные переводы Яндекс Сплит — отдельная задача Phase 6, механизм подбора сумм-пар ещё не проектировали.
- ФИО-регулярка — граница между ФИО и названием компании бывает размытой (ИП «Иванов И.И.»). Ждать данных из golden dataset.
- Как показать пользователю ретроспективно — какие правила были мигрированы из legacy, сколько активировано/отключено. Может стать отдельным экраном «Что изменилось».
- **Pre-existing failure** `tests/test_capital_snapshots.py::test_trend_calculation` (`NOT NULL constraint failed: capital_snapshots.net_capital`). Не связан с И-08 — тест и модель добавлены в `a316bf0` до Phase 0. Должен быть починен до релиза И-08, но не блокирует фазы.
- **Alembic autogenerate сломан.** Падает на resolution `transactions.goal_id → goals` (существующий баг конфигурации моделей, не связан с И-08). Это делает `alembic revision --autogenerate` непригодным до фикса — все миграции в рамках И-08 пишутся руками. Не блокирует фазы, но должен быть починен до релиза.
- **Phase 1.3 — bank_code как прокси через `source_type`.** Сейчас `fingerprint()` получает `bank = session.source_type` (csv/xlsx/pdf), что семантически неверно — это формат файла, не банк. Все CSV получают `bank="csv"` независимо от банка, теряется главный разделяющий признак. Настоящий `bank_code` появится в Phase 2 (при введении `scope=bank` у правил) или раньше, если Phase 3-кластеризация без него окажется бессмысленной. **Потенциальный блокер для acceptance Phase 3.2** («две строки Т-Банка и две строки Яндекс-Банка с одинаковым skeleton → разные кластеры»). Если до 3.2 не решим — делаем sub-задачу внутри 3.2.

## Риски всего плана

- **Объём.** 7 фаз — это много. Может растянуться на месяц+. Держать фазы мерджабельными по одной.
- **Сдвиг скоупа.** По ходу всплывёт желание делать «ещё и это». Любое такое — записывать в И-09…И-XX, не размазывать по текущему плану.
- **LLM-стоимость.** Пока пользователь один (ты) — неважно. При масштабе — сможет стать узким местом. Phase 6 мониторит.
- **Голос LLM.** Промпт нужно будет доводить: чтобы LLM не был слишком самоуверенным (именно то, от чего лечимся). Тестировать на фикс-наборе на склонность к галлюцинациям.

---

## Журнал изменений

- **2026-04-22** — **Two-bucket model + trust escalation.** Поверх trust-fix добавлена финальная UX-модель: два явных бакета на frontend — «Готово к импорту» (auto-trust, confidence ≥ 0.99) и «Требуют внимания» (всё остальное). **Gate для auto-trust** (`is_auto_trust`) — жёсткий: `rule_source='identifier'` + `identifier_match='matched'` + `rule.confirms ≥ AUTO_TRUST_MIN_CONFIRMS (5)` + `rule.rejections == 0` + `confidence ≥ AUTO_TRUST_THRESHOLD (0.99)`. Bank/legacy правила никогда не попадают в auto-trust — это и есть пожизненный инвариант доверия. **Proven tier (_CONF_EXACT_RULE_PROVEN = 0.99)** — новая вершина для exact-match с историей; `_compute_confidence` получил параметр `skip_singleton_drag=True` для proven tier (история правила покрывает слабость singleton-кластера). `Cluster` добавил property `auto_trust` + поле в `to_dict`. Endpoint `/moderation-status` теперь возвращает `auto_trust_rows/attention_rows` счётчики. **Frontend**: `ModerationClusterEntry` + `ModerationStatusResponse` расширены; в `ImportModerationPanel` появился новый `ClusterBuckets` компонент — делит карточки на два блока (зелёная шапка «Готово к импорту» + обычный attention-список). Макет обновлён до v6 с индикатором роста автоматизации («68% автоматически · было 10% после первого импорта»). **Эффект:** новичок видит 2 готовых / 20 требуют внимания, опытный 21/1 — это и есть виральная цель «инвестируй время в разметку, ускоришься в будущем». 6 новых тестов в `TestAutoTrustBucket`. Backend 196 passed / 5 skipped / 0 failed. `tsc --noEmit` OK.
- **2026-04-22** — **Trust-fix (живое расширение поверх Phase 3/4/5).** Пользователь обнаружил классическую «ложную уверенность»: правило, созданное на договоре ДГ-12345 (`scope=exact, identifier_value='ДГ-12345'`), срабатывало на новом договоре ДГ-99999 — потому что `_apply_rule` в path 3 (legacy) звал `get_best_rule(normalized_description=skeleton)`, который ходил по общему skeleton без учёта identifier. Итог — confidence 92%, карточка зелёная, пользователь скроллит и не замечает. **Фикс в 3 меры:** (M3) новый репо-метод `get_active_legacy_rule` — строго `identifier_value IS NULL`, чтобы exact-правила не утекали в legacy path; path 3 в `_apply_rule` переключён на него. (M1) `_compute_confidence` теперь принимает `identifier_match: matched/absent/unmatched` и множит на `_ID_MATCH_FACTORS` (matched=1.0, absent=1.0, **unmatched=0.60** — хардовая просадка). `_classify_identifier_match` сравнивает `bucket.identifier_value` с `rule.identifier_value` (для path 2/3). (M2) введены константы `ZONE_GREEN/YELLOW/RED` и `classify_trust_zone(confidence)` с порогами 0.85/0.65; `Cluster` получил поля `identifier_match/rule_confirms/rule_rejections/trust_zone`. Endpoint `/moderation-status` теперь отдаёт эти поля (pull через `ImportClusterService.build_clusters` при запросе, склейка с персистентными гипотезами по fingerprint). **Frontend:** новые типы `TrustZone/IdentifierMatch`, `ModerationClusterEntry` расширен. `ClusterCard` красится по зоне (emerald/amber/rose рамка), появился `ZoneBadge` («Готово / Проверь / Нужен ответ») и `TrustSignal` — микро-строка под гипотезой: «✓ Тот же идентификатор ДГ-12345, что и 14 раз раньше» / «⚠ Новый идентификатор ДГ-99999 — похоже по шаблону, но ты такое ещё не размечал». **Эффект:** кластер с новым контрактом больше не попадает в зелёную зону; confidence 0.78 × 0.95 × 0.60 = 0.44 → `red`. Пользователь теперь видит конкретный сигнал недоверия, а не абстрактное «65%». Макет `cluster-card-mockup.html` обновлён до v5. 5 новых юнит-тестов в `TestIdentifierAwareTrust`. Backend 190 passed / 5 skipped / 0 failed, `tsc --noEmit` OK.
- **2026-04-22** — **Phase 6 завершена (6.1, 6.2, 6.5 сделаны; 6.3, 6.4 отложены с зафиксированным обоснованием).** **6.1 (метрики):** новый `app/services/moderation_metrics_service.py::ModerationMetricsService.compute_for_user()` — агрегирует через `ImportSession.summary_json["moderation"]["tokens"]` (токены, llm_calls) и через `ImportRow.normalized_data_json["moderation"]["hypothesis"]["confidence"]` (распределение по бэндам ≥0.9 silent / ≥0.7 oneclick / <0.7 followup, среднее), + parked-rows count. Frozen dataclass `ModerationMetrics` с computed-свойствами `silent_share/oneclick_share/followup_share/avg_tokens_per_session` и `to_dict()`. Endpoint `GET /imports/moderation-metrics`. **Capture token usage:** в `ImportModeratorService` добавлен `moderate_cluster_with_usage()` возвращающий `(ClusterHypothesis, LLMResult)`; `moderate_import_session.py` использует новый метод и аккумулирует токены через `_accumulate_token_usage(session, llm_result)` — поля `input/output/cache_read/cache_creation/llm_calls` складываются пер-вызов в `summary_json["moderation"]["tokens"]`. Старый `moderate_cluster()` сохранён как фасад для обратной совместимости тестов. **6.2 (E2E):** `tests/test_moderation_pipeline_e2e.py` — полный pipeline на синтетических rows (normalizer_v2 → ImportClusterService → ImportModeratorService с `_FakeProvider`), плюс проверка что контракты-разделители fingerprint-ят в разные кластеры. Golden raw (Phase 1.4) ещё pending — реальный прогон будет по мере наполнения. **6.3 (пороги):** отложены без изменений — стартовые значения из Phase 2 (`RULE_ACTIVATE_CONFIRMS=2, RULE_GENERALIZE_CONFIRMS=3, RULE_DEACTIVATE_REJECTIONS=3, RULE_ERROR_RATIO_CAP=0.3`) и из Phase 3 (`CONFIDENCE_SILENT=0.9, CONFIDENCE_ONECLICK=0.7`). Калибровка требует реальных сессий модератора, менять числа «вслепую» рискованно. **6.4 (split-transfers):** отложено в отдельную задачу (И-09) — механизм подбора сумм-пар не проектировался, риск ложных срабатываний. **6.5 (honest-warning):** два теста в E2E-файле проверяют (a) что `follow_up_question` при confidence<0.7 проходит до вызывающего, (b) что галлюцинированный category_id обнуляется с сохранением остальных полей гипотезы (не тихая промоция, а явное снятие ответа). Backend: 185 passed / 5 skipped / 0 failed (+12 новых тестов).
- **2026-04-22** — **Phase 5 завершена (5.1–5.7).** **Расширены типы и API-клиенты:** `CategoryRule` получил все поля из 2.1 (`rejections/scope/is_active/bank_code/account_id_scope/identifier_key/identifier_value/created_at/updated_at`); новые типы `ModerationStatusResponse/ClusterHypothesis/ModerationClusterEntry/ParkedQueueItem/ParkedQueueResponse`. `getCategoryRules(filters)` передаёт `?scope=&is_active=`. Новые клиенты: `getModerationStatus/startModeration/parkImportRow/unparkImportRow/getParkedQueue`. **5.5 (Мои правила):** `app/(app)/rules/page.tsx` переписана — stat-карточки (total/active/inactive/legacy), фильтры по scope и is_active через Select, read-only таблица с scope-бейджем и индикатором активности. Toggle/delete отложены до Phase 7 (backend endpoints отсутствуют). **5.6 (parked queue):** `app/(app)/parked-queue/page.tsx` — global queue across sessions, кнопка «Вернуть в разбор» вызывает `unparkImportRow` → инвалидирует queryKey. **5.7 (счётчик в сайдбаре):** в `nav-items.ts` добавлен leaf `/parked-queue` с `badge: 'parked-queue'`; sidebar через `useParkedQueueCount` (refetchInterval=60s) показывает бейдж с числом. **5.1–5.4 (review в wizard):** создан `components/import/import-moderation-panel.tsx` — один компонент, включающий запуск (`POST /moderate`), polling (`refetchInterval`, 2s пока `pending|running`, затем стоп), прогресс-бар, карточки кластеров с hypothesis-блоком (operation_type/direction/category/confidence/reasoning/follow_up_question), цветовой код confidence (≥0.9 emerald / ≥0.7 amber / <0.7 rose). Кнопка «Отложить» парковает все строки кластера через `Promise.all`. Fallback-состояния: `failed` → warning-баннер с error, `skipped` → нейтральная подсказка. Панель встраивается в `import-wizard.tsx` как секция между preview и commit (preview→review→commit — три секции вместо двух), параллельный рефреш preview через `onClustersChanged`. Backend 173 passed / 5 skipped / 0 failed; `tsc --noEmit` OK на всём фронтенде.
- **2026-04-22** — **Phase 4 завершена (4.1–4.8).** **4.1 (рефакторинг LLM):** новая структура `app/services/llm/` — `base.py` с ABC `LLMProvider` + `LLMResult` + `LLMUnavailableError`, `anthropic_provider.py` c Anthropic SDK за провайдером, `registry.py` с `get_provider()` (lru_cache, пока жёстко AnthropicProvider — переключение на другие вендоры — при появлении второго). `llm_service.py` (domain-layer) теперь не знает про Anthropic, дёргает `get_provider().generate_structured(...)`. **4.2 (moderator):** `app/services/import_moderator_service.py::ImportModeratorService.moderate_cluster(cluster, context) -> ClusterHypothesis | None`; `ClusterHypothesis` (Pydantic) включает `operation_type`, `direction`, `predicted_category_id`, `confidence`, `reasoning`, `follow_up_question`; категория, не принадлежащая пользователю, обнуляется с сохранением остальных полей. **4.3 (хранение):** гипотеза пишется в `ImportRow.normalized_data_json["moderation"]` первой строки кластера (runtime-кластеры из 3.2 не персистятся, таблицу не создаём). **4.4 (Celery):** `app/jobs/moderate_import_session.py::moderate_import_session(session_id)` — билдит кластеры, вызывает LLM только для `confidence < 0.7` (`MODERATION_CONFIDENCE_CUTOFF=0.7`), остальные помечает skipped; состояние сессии в `summary_json["moderation"]` с полями `status/total_clusters/processed_clusters/started_at/finished_at/error`; ретраи не включены (сожгут токены тихо). **4.5 (endpoints):** `GET /imports/{id}/moderation-status` (возвращает session-level + per-cluster в одном ответе), `POST /imports/{id}/moderate` (kick off + enqueue Celery). **4.6 (fallback):** трёхуровневый — (a) провайдер disabled → сессия `skipped` целиком, (b) cluster-call вернул None/raise LLMUnavailableError → кластер `skipped`, (c) исключение в таске → сессия `failed` с error в summary; wizard работает всегда. **4.7 (анонимизация):** в промпт уходит только `cluster.skeleton` (плейсхолдеры `<PHONE>/<CONTRACT>/<IBAN>/<CARD>/<PERSON>/<ORG>/<AMOUNT>/<DATE>` уже проставлены в Phase 1) + `identifier_key` (тип — `phone`/`contract`/...), НО НЕ `identifier_value` (сам номер). Юнит-тест `test_raw_identifier_value_not_in_prompts` проверяет, что raw-значение не появляется в combined system+user payload. **4.8 (caching):** `cache_key="moderator:v1:{user_id}"` передаётся в `generate_structured`, `AnthropicProvider` вешает `cache_control: ephemeral` на system-блок когда cache_key не None. 28 новых тестов (`test_llm_provider.py` — 9, `test_import_moderator_service.py` — 10+), всего 173 passed / 5 skipped / 0 failed.
- **2026-04-22** — **3.8 завершена.** `app/services/refund_matcher_service.py` — pure-сервис без DB-стороны: принимает список row-dict'ов, возвращает `list[RefundMatch]`. Окно ±14 дней, amount+direction гард. Три уровня confidence: **0.95** (совпал contract/person_hash/counterparty_org ИЛИ refund-keyword «возврат/refund/отмена/chargeback» в описании любой стороны), **0.80** (совпал skeleton), **0.60** (только amount+window, `MIN_CONFIDENCE` cutoff). Greedy 1-to-1: сортировка pair-ов по confidence desc, каждая строка в максимум одной паре (не двойное счёт). Малформед-строки (нет amount/direction/date) тихо пропускаются. 13 тестов (`tests/test_refund_matcher.py`) зелёные. Интеграция в wizard-pipeline — Phase 5.
- **2026-04-22** — **3.1–3.4 завершены.** `app/services/import_cluster_service.py` — runtime-группировка (не таблица, как и планировалось в 3.1): `build_clusters(session) -> list[Cluster]` бежит по `ImportRow`, бакетит по `normalized_data.fingerprint`, агрегирует `count/total_amount/example_row_ids`, ранжирует. Правило-матчинг приоритет (3.3): `identifier` (exact по phone/contract/iban/card/person_hash) → `bank` (активное bank-scope правило по `bank_code + skeleton`) → `normalized_description` (legacy rule, активное) → `none`. Confidence (3.4): `base × error_ratio_factor × evidence_factor`, где base = 0.92/0.85/0.78 по источнику, error_ratio = confirms/(confirms+rejections), evidence penalty 0.05 для singleton-кластеров. Два новых метода в `TransactionCategoryRuleRepository`: `get_active_rule_by_identifier`, `get_active_rule_by_bank`. Service read-only — не пишет ни правила, ни строки. Frozen dataclass `Cluster` с `.to_dict()` для API-сериализации. Rows без fingerprint становятся singleton-кластерами `no-fp:{row_id}` — остаются видимы модератору. Сортировка кластеров: confidence desc → count desc → fingerprint. 15 тестов (`tests/test_import_cluster_service.py`) зелёные.
- **2026-04-22** — **3.5–3.7 завершены.** Статус `parked` — просто новое строковое значение в `ImportRow.status` (VARCHAR(32), не enum — миграция не нужна). `commit_import` пропускает parked-строки до любой логики создания транзакций (первая ветка в цикле), инкрементит `parked_count` в summary. `_build_summary_from_rows` добавил `parked_rows` счётчик, parked учитывается в `skipped_rows`. Аналитика (3.7) читает только `Transaction`, не `ImportRow` — парковка автоматически исключает строку из Потока/FI/DTI/Health/Buffer (инвариант: parked никогда не становится транзакцией, комментарий в `commit_import` явно фиксирует это решение). Service-методы `park_row/unpark_row/list_parked_queue` в `ImportService`: park переводит в `parked` (гард: нельзя парковать уже-committed строку), unpark возвращает в `warning` с `review_required=True` (гард: только parked можно unpark'ать). Endpoints `POST /imports/rows/{id}/park`, `POST /imports/rows/{id}/unpark`, `GET /imports/parked-queue` (глобальный список across sessions). Новый метод `ImportRepository.list_parked_queue`. 10 тестов (`tests/test_parked_rows.py`) зелёные.
- **2026-04-22** — **2.5 завершена.** `GET /category-rules` расширен: фильтры `scope` и `is_active` (query params), ответ включает все новые поля (`rejections`, `scope`, `is_active`, `bank_code`, `account_id_scope`, `identifier_key/value`, `created_at/updated_at`). Новый метод `list_rules(user_id, scope=None, is_active=None)` в `TransactionCategoryRuleRepository` — сортировка: активные → больше confirms → свежее. Старый `list_with_labels` сохранён для обратной совместимости.
- **2026-04-22** — **2.4 завершена.** Три точки учёта confirm/reject подключены к `RuleStrengthService`: (1) `ImportService._create_preview_row` — при совпадении правила сохраняет `applied_rule_id` и `applied_rule_category_id` в `normalized_data_json`; (2) `ImportService.update_row` — если пользователь меняет `category_id` на значение, отличное от того что предложило правило, вызывает `on_rejected(applied_rule_id)` и чистит ключи из normalized; (3) `ImportService.commit_import` — если `applied_rule_id` присутствует и категория не изменена → `on_confirmed`, иначе fallback на legacy `upsert` (для строк без prior-rule-match); (4) `TransactionService._maybe_reject_rule_on_edit` — при правке транзакции в окне ≤7 дней ищет связанную `ImportRow` через `ImportRepository.get_row_by_transaction_id`, при смене категории вызывает `on_rejected`. `RuleNotFound` везде проглатывается (правило удалено между preview и действием). Импорты `ImportRepository` и `RuleStrengthService` вынесены на уровень модуля в `transaction_service.py`. 12 новых тестов (`tests/test_rule_strength_integration.py`), 72 существующих зелёные.
- **2026-04-22** — **2.3 завершена.** `app/services/rule_strength_service.py` — единственная точка мутации `confirms/rejections/is_active/scope`. `RuleStrengthService.on_confirmed(rule_id)` и `on_rejected(rule_id)` возвращают `RuleTransition` (frozen dataclass со свойствами `activated/deactivated/generalized`). Правила активации (`False → True`) — при `confirms ≥ RULE_ACTIVATE_CONFIRMS` И `rejections == 0` (rejected-правило нельзя тихо реактивировать подтверждением — только через UI Phase 5). Генерализация `exact → bank` — при `confirms ≥ RULE_GENERALIZE_CONFIRMS`, `error_ratio ≤ RULE_ERROR_RATIO_CAP`, заполненном `bank_code`; `legacy_pattern` не обобщается (bank-контекст появится в Phase 4). Деактивация — по абсолютному порогу rejections ИЛИ по error ratio. 9 unit-тестов (`tests/test_rule_strength_service.py`) зелёные. Коммит `d4bf713`.
- **2026-04-22** — **2.2 завершена.** Модель `TransactionCategoryRule` обновлена: добавлены мэппинги `rejections`, `scope`, `is_active`, `bank_code`, `account_id_scope` (FK с `ondelete=SET NULL`), `identifier_key`, `identifier_value`. Python-дефолты совпадают с server_defaults из 0043. `RULE_SCOPES` и `RULE_IDENTIFIER_KEYS` (frozensets) для валидации на Python-стороне. Репозиторий — только механика рефакторинга `hit_count → confirms`; бизнес-логика confirm/reject и перехода scope — 2.3. Smoke-test: репо читает 226 правил, 108 активны, `sample.confirms=6 scope=legacy_pattern is_active=True`. Коммиты `e1e2ee0` (refactor rename) + `36fe1c9` (model strength fields).
- **2026-04-22** — **2.1 завершена.** Alembic-миграция `0043_rule_strength_fields` добавляет 7 колонок в `transaction_category_rules`: `rejections`, `scope` (VARCHAR(32), не enum — enums в PG болезненно эволюционировать), `is_active`, `bank_code`, `account_id_scope` (FK → `accounts.id` с `ondelete=SET NULL`, не CASCADE — удаление счёта не вычищает правила), `identifier_key`, `identifier_value`. Три индекса: `(user_id, scope, is_active)`, `(user_id, identifier_key, identifier_value)`, `(bank_code)`. Data-migration внутри `upgrade()`: `is_active = (hit_count >= 5)` для rows в default legacy-state (гард от двойного применения). Производственный замер совпал со spec: 226 правил → 108 active / 118 inactive. Downgrade/upgrade цикл чист, данные сохраняются. `hit_count` в 2.1 не тронут. Коммит `635f0c2`.
- **2026-04-22** — **1.4 (infra-only) завершена, raw pending.** Создан `scripts/generate_golden_expected.py` — pure-helper: читает TSV `<direction>\t<description>` (stdin или `--input`), прогоняет `extract_tokens`/`normalize_skeleton`/`fingerprint`, печатает draft expected-JSON в stdout; **в `expected/` сам не пишет**, redirect — на пользователе. Идентификаторы внутри скрипта хешируются (`sha256[:16]`), ФИО/orgs сводятся к `"PRESENT"/null`, `amounts`/`dates` — в счётчики. Новый `tests/test_import_normalizer_v2_golden.py` параметризован по `expected/*.expected.json`, скипает параметр если `status=pending_raw_fixture` ИЛИ raw отсутствует ИЛИ loader для расширения raw пока не реализован; CSV-loader есть, PDF/XLSX — стабы-скипы под первую реальную выписку. Unit-тест `tests/test_generate_golden_expected.py` покрывает `parse_input_lines`/`generate_expected`/`main` на синтетическом inputе (6 зелёных). Все 5 golden-параметров скипаются (raw ещё нет). README fixtures обновлён: воркфлоу raw → TSV → генератор → draft expected → review → `status=ready`. Наполнение — когда появятся реальные выписки. Коммит `da12a7d`.
- **2026-04-21** — план создан. Все 7 дизайн-вопросов закрыты, фазы размечены.
- **2026-04-22** — **1.3 завершена.** `ImportService._apply_v2_normalization` (staticmethod) интегрирован в preview-цикл сразу после except-блока v1-нормализации, до summary/create_row. Additive через `NormalizedDataV2.merge_into` — v1-ключи не трогаются. Bank для fingerprint = `session.source_type` (csv/xlsx/pdf), fallback `"unknown"` — bank_code-колонки ещё нет (Phase 2/3 материал). Direction: `type → direction → "expense"`. Все исключения ловятся и логируются WARNING, строка проходит без v2-ключей. 4 интеграционных теста в `tests/test_import_service_v2_normalization.py` (cluster-инвариант, unknown-bank, failure-path через monkeypatch) — `pytest.importorskip("pypdf")` для локального скипа. Коммит `e4a268b`.
- **2026-04-22** — **1.2 завершена.** Создан `app/schemas/import_normalized.py`: Pydantic `NormalizedDataV2` + `TokensV2`. `from_tokens/from_normalized_data/from_import_row` + `merge_into` (additive, не мутирует вход). `person_name_present: bool` — без самой ФИО (приватность: поле уходит в UI/экспорты/логи). `amounts_extra`/`dates_extra` — встроенные в описание, отдельные от основной суммы строки (для multi-leg transfer). `normalizer_version=2`. Миграция Alembic не требуется — колонка уже JSON. 15 юнит-тестов проходят. Коммит `72c85c0`. Отдельная спека в `14-Specifications/` не создавалась по инструкции.
- **2026-04-22** — **1.1 завершена.** Создан `app/services/import_normalizer_v2.py` (pure-функции, без DB): `extract_tokens`, `normalize_skeleton`, `fingerprint` + frozen dataclass `ExtractedTokens`. Плейсхолдеры `<PHONE>/<CONTRACT>/<IBAN>/<CARD>/<PERSON>/<ORG>/<AMOUNT>/<DATE>`. Fingerprint — sha256[:16], contract включается только если не None. 36 синтетических юнит-тестов проходят. ФИО-regex сужен до фамилий ≥3 символов, широкий риск (ORG→PERSON) отложен на калибровку в 1.4. Pre-existing failure `test_capital_snapshots::test_trend_calculation` не связан с этим модулем (NOT NULL в `net_capital`, иная схема), на 1.1 не влияет. Коммит `0119e68`.
- **2026-04-22** — **0.3 завершена (инфра).** Создан `tests/fixtures/statements/{raw,expected}/` + README. `raw/` gitignored (приватность: принят вариант «(c)» — raw остаётся локально, expected с хешированными идентификаторами и `PRESENT`-метками для ФИО коммитится). Скелеты expected созданы для всех 5 сценариев (`status: "pending_raw_fixture"`). Наполнение — итеративно по ходу Phase 1.4, не блокирует Phase 1.1/1.2.
- **2026-04-22** — **0.2 завершена.** Создана ветка `feature/import-moderator` от чистого master (HEAD: `0730338`).
- **2026-04-22** — **0.1 завершена.** Разобраны 11 uncommitted изменений. Master очищен 9 коммитами: 4 bug fix (дедуп по типу, stale response_model, outside-click dropdown, transfer matcher + secondary-duplicate), 3 улучшения (token expire, keyword-based refund/credit-repayment — помечено «superseded in И-08 Phase 7», clickable status filters + debt_direction), docs И-08 (проблемы/идеи/план), chore untrack `financeapp-vault/.obsidian/workspace.json` (+ `.gitignore`). Откачены без коммита: `.claude/settings.local.json` (Bash(xxd)) и `pdf_extractor.py` (mojibake в регулярках — эксперимент вслепую, реальная работа начнётся в Phase 1 на fixture'ах). На будущее (не в И-08): проверить остальные `.obsidian/*.json` (app/appearance/core-plugins/graph) — тоже часто ephemeral state, кандидаты на untrack; это отдельный vault cleanup.
