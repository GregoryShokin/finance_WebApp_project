# Фаза 6 — Обновление метрик-карточек дашборда (2026-04-19)

> Промпт для Claude Code. Выполняется ПОСЛЕ Фазы 5.
>
> **Предусловие:** Фаза 5 завершена. `useMetricsSummary` работает, `MetricsSummary` тип обновлён (free_capital, buffer_stability и т.д.), `FlowWidget` стоит в SectionAnalytics.

**Результат фазы:** три метрики-карточки в верхней строке `SectionMoney` (`dashboard-new`) берут данные из `/metrics/summary`, а не из `/financial-health`. Карточка «Запас» переименована в «Буфер устойчивости» и показывает месяцы покрытия вкладами. Зависимость `dashboard-new` от `/financial-health` сведена к минимуму (только для CapitalWidget, CreditsWidget, DebtsWidget).

---

```
Ты выполняешь Фазу 6. Целевой дашборд: `frontend/app/(app)/dashboard-new/`.

## Что сейчас

`dashboard-new/page.tsx` вычисляет три метрики для SectionMoney:

```typescript
const flow = useMemo(() => (health ? computeFlow(transactions) : null), [transactions, health]);
const load = useMemo(() => (health ? computeLoad(health) : null), [health]);
const reserve = useMemo(() => (health ? computeReserve(goals, health) : null), [goals, health]);
```

- `computeFlow` — считает из транзакций самостоятельно (старая формула `income - expense`)
- `computeLoad` — берёт `health.dti` из `/financial-health`
- `computeReserve` — берёт данные цели `safety_buffer` из goals

Все три должны перейти на `/metrics/summary` — там правильные значения после Фаз 1-4.

## Задача

### Шаг 1. Переписать три compute-функции в dashboard-data.ts

Файл: `frontend/components/dashboard-new/dashboard-data.ts`

Добавь новые версии функций, принимающие `MetricsSummary`:

```typescript
import type { MetricsSummary } from '@/lib/api/metrics';

// Вместо computeFlow(transactions)
export function computeFlowFromSummary(summary: MetricsSummary): FlowMetric {
  const { basic_flow, lifestyle_indicator, zone } = summary.flow;
  const label = zone === 'healthy' ? 'В норме' : zone === 'tight' ? 'Впритык' : 'Дефицит';
  const tone: TagTone = zone === 'healthy' ? 'green' : zone === 'tight' ? 'amber' : 'red';
  return { balance: basic_flow, label, tone };
}

// Вместо computeLoad(health)
export function computeLoadFromSummary(summary: MetricsSummary): LoadMetric {
  const dti = summary.dti.dti_percent ?? 0;
  const zone = summary.dti.zone;
  const label = zone === 'normal' ? 'Низкая' : zone === 'acceptable' ? 'Допустимая' : 'Высокая';
  const tone: TagTone = zone === 'normal' ? 'green' : zone === 'acceptable' ? 'amber' : 'red';
  return { dti, label, tone };
}

// Вместо computeReserve(goals, health) — теперь это Буфер устойчивости
export type BufferMetric = { months: number; label: string; tone: TagTone };

export function computeBufferFromSummary(summary: MetricsSummary): BufferMetric {
  const months = summary.buffer_stability.months ?? 0;
  const zone = summary.buffer_stability.zone;
  const label =
    zone === 'excellent' ? 'Отличный'
    : zone === 'normal' ? 'Хороший'
    : zone === 'minimum' ? 'Минимальный'
    : 'Недостаточный';
  const tone: TagTone =
    zone === 'excellent' || zone === 'normal' ? 'green'
    : zone === 'minimum' ? 'amber'
    : 'red';
  return { months, label, tone };
}
```

Старые `computeFlow`, `computeLoad`, `computeReserve` оставь — они используются как fallback.

### Шаг 2. Обновить page.tsx

Файл: `frontend/app/(app)/dashboard-new/page.tsx`

Уже есть `metricsSummaryQuery` (добавлен в Фазе 5). Добавить:

```typescript
import {
  computeFlowFromSummary,
  computeLoadFromSummary,
  computeBufferFromSummary,
  type BufferMetric,
} from '@/components/dashboard-new/dashboard-data';

// Заменить три строки вычислений:
const metricsSummary = metricsSummaryQuery.data;

const flow = useMemo(
  () => metricsSummary ? computeFlowFromSummary(metricsSummary) : null,
  [metricsSummary],
);
const load = useMemo(
  () => metricsSummary ? computeLoadFromSummary(metricsSummary) : null,
  [metricsSummary],
);
const buffer = useMemo(
  () => metricsSummary ? computeBufferFromSummary(metricsSummary) : null,
  [metricsSummary],
);
```

Обновить условие рендера (убрать `reserve` из проверки, добавить `buffer`):

```typescript
{!isLoading && !isError && health && flow && load && buffer && safetyBuffer && capitalData ? (
  <div className="space-y-6">
    <SectionMoney
      flow={flow}
      load={load}
      buffer={buffer}    // ← было reserve
      ...
    />
    ...
```

### Шаг 3. Обновить SectionMoney

Файл: `frontend/components/dashboard-new/section-money.tsx`

В Props:
```typescript
type Props = {
  flow: FlowMetric;
  load: LoadMetric;
  buffer: BufferMetric;   // ← было reserve: ReserveMetric
  ...
};
```

Карточка «Запас» → «Буфер устойчивости»:

```tsx
{/* Card 3 -- Буфер устойчивости */}
<div className={CARD_CLASS}>
  <div className="flex items-center justify-between">
    <span className="text-[14px] font-semibold text-slate-900">Буфер</span>
    <span className={`inline-block px-2 py-0.5 rounded-full text-[11px] font-semibold ${TAG_CLASSES[buffer.tone]}`}>
      {buffer.label}
    </span>
  </div>
  <div className="text-2xl font-extrabold text-slate-900 mt-1">
    {buffer.months.toFixed(1)} мес.
  </div>
  <div className="text-xs text-slate-400 mt-1">
    Вклады / среднемесячные расходы
  </div>
</div>
```

### Шаг 4. Обновить isLoading

В `page.tsx`, добавить `metricsSummaryQuery.isLoading` в условие `isLoading`:

```typescript
const isLoading =
  metricsSummaryQuery.isLoading ||  // ← добавить
  healthQuery.isLoading ||
  accountsQuery.isLoading ||
  ...
```

### Шаг 5. Проверить что health использовать только там, где нужно

После перехода flow/load/buffer на MetricsSummary — `health` (`/financial-health`) нужен только для:
- `CapitalWidget` — передаёт `health`
- `CreditsWidget` — передаёт `health`
- `DebtsWidget` — передаёт `health`
- `safetyBuffer` expandable card — использует цель safety_buffer и health.avg_monthly_expenses

Убедись, что в условии рендера `health` всё ещё требуется (для виджетов капитала). Если убирать зависимость от health полностью — это следующий рефакторинг, не сейчас.

### Шаг 6. Удалить тип ReserveMetric из dashboard-data.ts если не используется

Проверь `grep -n "ReserveMetric" frontend/`. Если только в `dashboard-data.ts` и `section-money.tsx` — удаляй. Если где-то ещё — оставляй.

## Deliverables (чеклист)

- [ ] `computeFlowFromSummary`, `computeLoadFromSummary`, `computeBufferFromSummary` добавлены в `dashboard-data.ts`
- [ ] `page.tsx`: `flow`, `load`, `buffer` вычисляются из `MetricsSummary`
- [ ] `SectionMoney` принимает `buffer: BufferMetric` вместо `reserve: ReserveMetric`
- [ ] Карточка «Запас» переименована в «Буфер», показывает `buffer.months` и подпись «Вклады / среднемесячные расходы»
- [ ] `metricsSummaryQuery.isLoading` добавлен в условие загрузки
- [ ] `npm run build` — 0 TypeScript ошибок
- [ ] Ручная проверка: в браузере три карточки показывают правильные числа, совпадающие с `/metrics/summary` (проверить через DevTools)

## Что НЕ входит

- Expandable SafetyBuffer card (оставить из goals — это цель пользователя, не метрика)
- CapitalWidget, CreditsWidget, DebtsWidget (они остаются на health)
- Редирект /dashboard → /dashboard-new (Фаза 8)
```
