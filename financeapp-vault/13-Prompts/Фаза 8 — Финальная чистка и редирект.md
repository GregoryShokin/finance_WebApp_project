# Фаза 8 — Финальная чистка и редирект (2026-04-19)

> Промпт для Claude Code. Выполняется ПОСЛЕДНЕЙ, после Фаз 1-7.
>
> **Предусловие:** Фазы 1-7 завершены. `dashboard-new` — полностью функциональный, `dashboard` — старая версия.

**Результат фазы:** пользователи видят `dashboard-new` по всем точкам входа. Старый `/dashboard` либо редиректит, либо удалён. Нет сломанных ссылок. GAP #13 из спецификации закрыт.

---

```
Ты выполняешь последнюю фазу — финальная чистка.

## Что менять

### Шаг 1. Обновить навигацию — 2 файла

**Файл 1:** `frontend/components/layout/nav-items.ts`, строка ~41:
```typescript
// Было:
{ href: '/dashboard', label: 'Дашборд', icon: LayoutDashboard },
// Стало:
{ href: '/dashboard-new', label: 'Дашборд', icon: LayoutDashboard },
```

**Файл 2:** `frontend/components/layout/sidebar.tsx`, строка ~97:
```tsx
// Было:
<Link href="/dashboard" className="...">
// Стало:
<Link href="/dashboard-new" className="...">
```

### Шаг 2. Обновить middleware

**Файл:** `frontend/middleware.ts`, строка ~16:
```typescript
// Было:
return NextResponse.redirect(new URL(token ? '/dashboard' : '/login', request.url));
// Стало:
return NextResponse.redirect(new URL(token ? '/dashboard-new' : '/login', request.url));
```

### Шаг 3. Добавить редирект со старого роута

**Файл:** `frontend/app/(app)/dashboard/page.tsx`

Заменить содержимое на redirect:
```typescript
import { redirect } from 'next/navigation';

export default function DashboardRedirectPage() {
  redirect('/dashboard-new');
}
```

Это сохраняет роут `/dashboard` работоспособным (чтобы не ломать закладки пользователей), но сразу перебрасывает на новый.

### Шаг 4. grep на оставшиеся ссылки на /dashboard

```bash
grep -rn "href.*'/dashboard'" frontend/ --include="*.tsx" --include="*.ts" | grep -v dashboard-new | grep -v ".next"
grep -rn '"/dashboard"' frontend/ --include="*.tsx" --include="*.ts" | grep -v dashboard-new | grep -v ".next"
```

Для каждого найденного — заменить на `/dashboard-new` (кроме файла redirect-страницы, который мы только что создали).

### Шаг 5. Финальная проверка

- [ ] `npm run build` — 0 ошибок
- [ ] Открыть браузер, перейти по `/dashboard` — должен редиректить на `/dashboard-new`
- [ ] Кликнуть «Дашборд» в боковой навигации — открывается `/dashboard-new`
- [ ] После логина — попадаешь на `/dashboard-new`
- [ ] Кольцо FI-score на `/health` — 4 сегмента
- [ ] Виджет Потока в `/dashboard-new` — три таба переключаются

## Deliverables (чеклист)

- [ ] `nav-items.ts` — href обновлён на `/dashboard-new`
- [ ] `sidebar.tsx` — ссылка логотипа/домой обновлена на `/dashboard-new`
- [ ] `middleware.ts` — редирект после логина на `/dashboard-new`
- [ ] `/dashboard/page.tsx` — заменён на `redirect('/dashboard-new')`
- [ ] `grep "/dashboard"` — не возвращает ничего лишнего (кроме redirect-страницы и /dashboard-new)
- [ ] `npm run build` — 0 ошибок
- [ ] Ручная проверка в браузере: все точки входа ведут в `/dashboard-new`

## После Фазы 8

Всё. GAP-ы #1-5, #7-8 из спецификации закрыты. Открытые вопросы зафиксированы в:
- `financeapp-vault/20-Open-Questions/Валюта — модель потоков.md`
- `financeapp-vault/20-Open-Questions/Показатели готовности к инвестициям.md`

Следующие треки (за пределами текущего плана):
- Прогрессивная блокировка табов виджета Потока по модулям школы (когда система `unlocked_widgets` будет реализована)
- Project как сущность (GAP #7)
- Flutter-версия (Roadmap)
- Банковские интеграции (2027+)
```
